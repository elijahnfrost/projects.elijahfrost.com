{
  "name": "project-directory",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "mkdir -p api public && cp functions/api/projects.js api/projects.js && cp index.html style.css script.js public/"
  }
}
