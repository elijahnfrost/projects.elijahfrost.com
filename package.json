{
  "name": "project-directory",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "vercel-build": "mkdir -p api public && cp functions/api/projects.js api/projects.js && cp index.html style.css script.js cursor.js icon.svg public/"
  }
}
